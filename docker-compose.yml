services:
  frontend:
    build:
      context: .
      dockerfile: dockerfile.dev
    ports:
      - "5173:5173"
    environment:
      - VITE_API_URL=${VITE_API_URL}
    volumes:
      - ./front:/app
      - /app/node_modules